# HamHome i18n 架构总览

## 🏗️ 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                      React Components                            │
│  (MainContent, BookmarkCard, SettingsPage, etc.)               │
│                                                                  │
│  const { t } = useTranslation('bookmark')                      │
│  <h1>{t('bookmark.title')}</h1>                                │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   react-i18next Provider                        │
│              <I18nextProvider i18n={i18n}>                      │
│                                                                  │
│  - 提供翻译上下文给子组件                                      │
│  - 管理语言切换事件                                            │
│  - 缓存翻译结果                                                │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    i18next Core Instance                         │
│  (lib/i18n/config.ts + lib/i18n/init.ts)                       │
│                                                                  │
│  - 管理语言状态                                                │
│  - 加载和切换翻译资源                                          │
│  - 处理参数插值和复数形式                                      │
│  - 与 LocalStorage 同步用户选择                                │
└────────────────────────┬────────────────────────────────────────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
         ▼               ▼               ▼
    ┌────────┐   ┌────────┐    ┌────────┐
    │ common │   │bookmark│    │settings│
    │.json   │   │.json   │    │.json   │
    │(zh/en) │   │(zh/en) │    │(zh/en) │
    └────────┘   └────────┘    └────────┘

         │
         ▼
    ┌────────┐
    │  ai    │
    │.json   │
    │(zh/en) │
    └────────┘
```

## 🔄 语言切换流程

```
┌──────────────────────────────────────────────────┐
│ 用户操作：在设置页面选择语言                     │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
        ┌────────────────────────────┐
        │ useLanguage() Hook          │
        │ switchLanguage(lng)         │
        └────────────────┬────────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
         ▼                               ▼
    ┌─────────────────┐         ┌──────────────────┐
    │ i18n.changeL…() │         │ updateAppSetting│
    │ 切换翻译资源    │         │ 保存到本地配置   │
    └────────┬────────┘         └────────┬─────────┘
             │                          │
             ├──────────────┬───────────┘
             │              │
             ▼              ▼
    ┌─────────────────────────────────────────┐
    │ localStorage 更新                       │
    │ key: 'i18nextLng', value: 'zh' | 'en'  │
    └────────────────┬────────────────────────┘
                     │
                     ▼
    ┌─────────────────────────────────────────┐
    │ 触发自定义事件：languageChange          │
    │ window.dispatchEvent(new CustomEvent)   │
    └────────────────┬────────────────────────┘
                     │
                     ▼
    ┌─────────────────────────────────────────┐
    │ 所有监听翻译的组件 re-render            │
    │ UI 文本立即更新为新语言                 │
    └─────────────────────────────────────────┘
```

## 📦 包结构关系

```
ham_home (monorepo)
│
├── apps/
│   │
│   ├── extension/
│   │   ├── lib/i18n/
│   │   │   ├── config.ts ............ i18n 实例配置
│   │   │   └── init.ts ............ 初始化函数
│   │   │
│   │   ├── hooks/
│   │   │   └── useLanguage.ts ....... 语言管理 Hook
│   │   │
│   │   └── locales/
│   │       ├── en/
│   │       │   ├── common.json
│   │       │   ├── bookmark.json
│   │       │   ├── settings.json
│   │       │   └── ai.json
│   │       └── zh/
│   │           ├── common.json
│   │           ├── bookmark.json
│   │           ├── settings.json
│   │           └── ai.json
│   │
│   └── web/
│       ├── lib/i18n/ ............ 同样的配置
│       └── locales/ ............ 同样的翻译
│
├── packages/
│   └── i18n/ (可选的共享包)
│       ├── src/
│       │   ├── index.ts
│       │   ├── config.ts
│       │   └── types.ts
│       └── package.json
│
└── docs/
    ├── i18n_implementation_plan.md .... 完整方案
    ├── i18n_integration_guide.md ...... 集成指南
    ├── i18n_quick_reference.md ....... 快速参考
    └── I18N_SUMMARY.md .............. 总结
```

## 🎯 命名空间划分

```
┌─────────────────────────────────────────┐
│        i18n 资源映射（4 个命名空间）    │
├─────────────────────────────────────────┤
│                                         │
│ ┌─ common ─────────────────┐           │
│ │ 通用操作和反馈           │           │
│ │ - 按钮：save, cancel ... │           │
│ │ - 状态：loading, error   │           │
│ │ - 列表：empty, search    │           │
│ └─────────────────────────┘           │
│                                        │
│ ┌─ bookmark ───────────────┐          │
│ │ 书签相关功能            │          │
│ │ - 操作：new, edit, del  │          │
│ │ - 分类/标签管理         │          │
│ │ - 搜索和统计            │          │
│ └─────────────────────────┘          │
│                                        │
│ ┌─ settings ───────────────┐          │
│ │ 用户设置和配置          │          │
│ │ - 语言、主题、AI 设置   │          │
│ │ - 导入导出功能          │          │
│ │ - 关于页面              │          │
│ └─────────────────────────┘          │
│                                        │
│ ┌─ ai ─────────────────────┐          │
│ │ AI 相关功能和提示       │          │
│ │ - 分析状态提示          │          │
│ │ - 错误和成功消息        │          │
│ │ - 配置相关提示          │          │
│ └─────────────────────────┘          │
│                                        │
└─────────────────────────────────────────┘
```

## 🌍 语言支持扩展路径

```
阶段 1: 基础支持（当前）
┌──────────────┬──────────────┐
│   English    │  简体中文    │
│   (en)       │   (zh)       │
└──────────────┴──────────────┘

阶段 2: 主要市场（推荐）
┌──────────────┬──────────────┬──────────────┐
│   English    │  简体中文    │   日本語     │
│   (en)       │   (zh)       │   (ja)       │
└──────────────┴──────────────┴──────────────┘
                    ↓
                ┌─────────┐
                │ 한국어  │
                │  (ko)   │
                └─────────┘

阶段 3: 全球覆盖（未来）
加入：Spanish, French, German, Russian, etc.
```

## 📊 翻译键分布

```
common.json
├── 基础操作 (8)
│   └── save, cancel, delete, edit, close, confirm, back, next
├── 状态提示 (5)
│   └── loading, error, success, warning, empty
├── 列表操作 (2)
│   └── search, noResults
└── 语言列表 (4)
    └── en, zh, ja, ko

bookmark.json
├── 基础操作 (5)
│   └── newBookmark, addBookmark, editBookmark, deleteBookmark
├── 反馈提示 (4)
│   └── deleteConfirm, deleteSuccess, saveSuccess, saveFailed
├── 占位符 (3)
│   └── title, description, url
├── 分类标签 (4)
│   └── categories, uncategorized, tags, createdAt
├── 显示视图 (4)
│   └── recent, all, search, title
└── 统计信息 (2)
    └── count, count_plural

settings.json (22)
├── 语言主题 (5)
├── AI 设置 (8)
├── 导入导出 (2)
└── 按钮和描述 (7)

ai.json (28)
├── 处理状态 (4)
├── 推荐信息 (2)
├── 错误处理 (7)
└── 成功提示 (4) + 配置信息

总计: 87 个翻译键
```

## 🔄 数据流向

```
User Action (语言切换)
    │
    ▼
useLanguage Hook
    ├─→ switchLanguage(lng)
    │
    ├─→ i18n.changeLanguage(lng)
    │   ├─→ 更新当前语言状态
    │   └─→ 加载对应语言资源
    │
    ├─→ updateAppSettings({ language: lng })
    │   └─→ 保存到本地配置
    │
    └─→ localStorage.setItem('i18nextLng', lng)
        └─→ 持久化用户选择
            │
            ▼
        Window Event Dispatch
            │
            ▼
    所有订阅翻译的组件 re-render
            │
            ▼
        UI 显示新语言文本
```

## 📱 组件使用示例

```
┌────────────────────────────────────┐
│      React Component                │
│  (需要显示翻译文本)                 │
└────────────────────┬───────────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │ useTranslation Hook    │
        │ const { t } = useT...  │
        │   ('namespace')        │
        └────────────┬───────────┘
                     │
         ┌───────────┼──────────┐
         │           │          │
         ▼           ▼          ▼
    ┌────────┐  ┌────────┐  ┌──────────┐
    │ 简单键 │  │ 参数化 │  │ 复数形式 │
    │        │  │        │  │          │
    │t('key')│  │t('key',│  │t('key',  │
    │        │  │{var})  │  │{count})  │
    └────────┘  └────────┘  └──────────┘
         │           │          │
         └───────────┴──────────┘
                     │
                     ▼
        从翻译文件查找对应值
                     │
                     ▼
            返回翻译后的文本
                     │
                     ▼
            渲染到 DOM 中
```

## ⚙️ 配置初始化流程

```
应用启动
  │
  ├─→ main.tsx 执行
  │     │
  │     └─→ await i18n.init()
  │           │
  │           ├─→ i18next-browser-languagedetector
  │           │   ├─ 检查 localStorage
  │           │   ├─ 检查 sessionStorage
  │           │   ├─ 检查 cookie
  │           │   ├─ 检查 navigator.language
  │           │   └─ 使用 fallback 语言
  │           │
  │           ├─→ 加载对应语言资源
  │           │   ├─ common.json
  │           │   ├─ bookmark.json
  │           │   ├─ settings.json
  │           │   └─ ai.json
  │           │
  │           └─→ 初始化完成
  │
  ├─→ <I18nextProvider i18n={i18n}>
  │   └─→ 包装整个应用
  │
  ├─→ 组件树渲染
  │   ├─→ useTranslation 获取翻译函数
  │   ├─→ 调用 t() 获取翻译文本
  │   └─→ 显示本地化的 UI
  │
  └─→ 应用就绪
```

---

这个架构图提供了完整的可视化理解，包括：
1. **系统架构** - 各层的交互关系
2. **语言切换流程** - 用户操作到 UI 更新的完整路径
3. **包结构** - 文件组织和依赖
4. **命名空间划分** - 翻译资源的逻辑分组
5. **语言扩展路径** - 如何支持更多语言
6. **翻译键分布** - 各个模块的内容量
7. **数据流向** - 信息流动的方向
8. **组件使用** - 组件如何使用翻译
9. **初始化流程** - 应用启动时的初始化步骤
